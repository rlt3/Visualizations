function love.conf (t)
    t.gammacorrect = true
    t.window.title = "Sphere"
    -- Filepath to an image to use as the window's icon (string)
    t.window.icon = nil
    t.window.width = 600
    t.window.height = 600
    -- window's position on screen
    --t.window.x = 0
    --t.window.y = 0
end
